apiVersion: apps/v1
kind: Deployment

metadata:
  name: web-server

spec:
  replicas: 1

  selector:
    matchLabels:
      app: web-server

  template:
    metadata:
      labels:
        app: web-server

    spec:
      containers:
        - name: web-server

          image: 145023121102.dkr.ecr.ap-south-1.amazonaws.com/web:2

          imagePullPolicy: Always

          ports:
            - containerPort: 80
